version: "3.9"

services:
  ds-site:
    build: .
    container_name: ds-site
    ports:
      - "5554:80"
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    networks:
      - proxy_network

networks:
  proxy_network:
    external: true
